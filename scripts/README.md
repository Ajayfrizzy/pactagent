# Repository scripts

Scripts in this directory operate across workspaces or support deployment,
policy, environment setup, backup, and production validation. Workspace-specific
maintenance commands belong with the workspace that owns them.

Use verb-first kebab-case names and add regularly used commands to the root
`package.json` so contributors do not need to know implementation paths.
