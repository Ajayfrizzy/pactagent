# Server maintenance scripts

Commands here depend directly on server configuration, data, or API artifacts.
Expose commonly used commands through `server/package.json` and use kebab-case
for new script filenames.

Repository-wide deployment, policy, backup, and environment commands belong in
the root `scripts/` directory.
